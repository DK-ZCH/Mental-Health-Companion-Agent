<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.XinlilaoshiDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.username as username
        ,a.password as password
        ,a.name as name
        ,a.phone as phone
        ,a.avatar_url as avatarUrl
        ,a.gender as gender
        ,a.email as email
        ,a.expertise as expertise
        ,a.resume as resume
        ,a.introduction as introduction
        ,a.created_at as createdAt
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.XinlilaoshiView" >
        SELECT
        <include refid="Base_Column_List" />

--         级联表的字段

        FROM counselor  a

        <where>
            <if test="params.ids != null">
                and a.id in
                <foreach item="item" index="index" collection="params.ids" open="(" separator="," close=")">
                #{item}
                </foreach>
            </if>
            <if test=" params.username != '' and params.username != null and params.username != 'null' ">
                and a.username like CONCAT('%',#{params.username},'%')
            </if>
            <if test=" params.password != '' and params.password != null and params.password != 'null' ">
                and a.password like CONCAT('%',#{params.password},'%')
            </if>
            <if test=" params.name != '' and params.name != null and params.name != 'null' ">
                and a.name like CONCAT('%',#{params.name},'%')
            </if>
            <if test=" params.phone != '' and params.phone != null and params.phone != 'null' ">
                and a.phone like CONCAT('%',#{params.phone},'%')
            </if>
            <if test="params.gender != null and params.gender != ''">
                and a.gender = #{params.gender}
            </if>
            <if test=" params.email != '' and params.email != null and params.email != 'null' ">
                and a.email like CONCAT('%',#{params.email},'%')
            </if>
            <if test=" params.expertise != '' and params.expertise != null and params.expertise != 'null' ">
                and a.expertise like CONCAT('%',#{params.expertise},'%')
            </if>
            <if test=" params.resume != '' and params.resume != null and params.resume != 'null' ">
                and a.resume like CONCAT('%',#{params.resume},'%')
            </if>
            <if test=" params.introduction != '' and params.introduction != null and params.introduction != 'null' ">
                and a.introduction like CONCAT('%',#{params.introduction},'%')
            </if>

        </where>

        order by a.${params.orderBy} desc 
    </select>

</mapper>