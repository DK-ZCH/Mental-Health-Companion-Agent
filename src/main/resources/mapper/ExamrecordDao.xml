<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.ExamrecordDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.record_no as recordNo
        ,a.student_id as studentId
        ,a.paper_id as paperId
        ,a.total_score as totalScore
        ,a.submitted_at as submittedAt
        ,a.created_at as createdAt
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.ExamrecordView" >
        SELECT
        <include refid="Base_Column_List" />

--         级联表的字段
        ,assessment_paper.name as paperName
        ,assessment_paper.duration_minutes as paperDurationMinutes
        ,assessment_paper.total_score as paperTotalScore
        ,assessment_paper.status as paperStatus
        ,assessment_paper.is_deleted as paperIsDeleted
        ,student.name as studentName
        ,student.phone as studentPhone
        ,student.id_card_no as studentIdCardNo
        ,student.avatar_url as studentAvatarUrl
        ,student.email as studentEmail

        FROM assessment_record  a
        left JOIN assessment_paper assessment_paper ON a.paper_id = assessment_paper.id
        left JOIN student student ON a.student_id = student.id

        <where>
            <if test="params.ids != null">
                and a.id in
                <foreach item="item" index="index" collection="params.ids" open="(" separator="," close=")">
                #{item}
                </foreach>
            </if>
            <if test=" params.recordNo != '' and params.recordNo != null and params.recordNo != 'null' ">
                and a.record_no like CONCAT('%',#{params.recordNo},'%')
            </if>
            <if test="params.studentId != null and params.studentId != ''">
                and (
                    a.student_id = #{params.studentId}
                )
            </if>
            <if test="params.paperId != null and params.paperId != ''">
                and (
                    a.paper_id = #{params.paperId}
                )
            </if>
            <if test="params.totalScoreStart != null and params.totalScoreStart != ''">
                <![CDATA[  and a.total_score >= #{params.totalScoreStart}   ]]>
            </if>
            <if test="params.totalScoreEnd != null and params.totalScoreEnd != ''">
                <![CDATA[  and a.total_score <= #{params.totalScoreEnd}   ]]>
            </if>
             <if test="params.totalScore != null and params.totalScore != ''">
                and a.total_score = #{params.totalScore}
             </if>
            <if test=" params.submittedAtStart != '' and params.submittedAtStart != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.submitted_at) >= UNIX_TIMESTAMP(#{params.submittedAtStart}) ]]>
            </if>
            <if test=" params.submittedAtEnd != '' and params.submittedAtEnd != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.submitted_at) <= UNIX_TIMESTAMP(#{params.submittedAtEnd}) ]]>
            </if>

                <!-- 判断试卷表的id不为空 -->
            <if test=" params.exampaperIdNotNull != '' and params.exampaperIdNotNull != null and params.exampaperIdNotNull != 'null' ">
                and a.paper_id IS NOT NULL
            </if>
            <if test=" params.paperName != '' and params.paperName != null and params.paperName != 'null' ">
                and assessment_paper.name like CONCAT('%',#{params.paperName},'%')
            </if>
            <if test="params.exampaperDateStart != null  and params.exampaperDateStart != '' ">
                <![CDATA[  and assessment_paper.duration_minutes >= #{params.exampaperDateStart}   ]]>
            </if>
            <if test="params.exampaperDateEnd != null  and params.exampaperDateEnd != '' ">
                <![CDATA[  and assessment_paper.duration_minutes <= #{params.exampaperDateEnd}   ]]>
            </if>
            <if test="params.paperDurationMinutes != null  and params.paperDurationMinutes != '' ">
                and assessment_paper.duration_minutes = #{params.paperDurationMinutes}
            </if>
            <if test="params.exampaperMyscoreStart != null  and params.exampaperMyscoreStart != '' ">
                <![CDATA[  and assessment_paper.total_score >= #{params.exampaperMyscoreStart}   ]]>
            </if>
            <if test="params.exampaperMyscoreEnd != null  and params.exampaperMyscoreEnd != '' ">
                <![CDATA[  and assessment_paper.total_score <= #{params.exampaperMyscoreEnd}   ]]>
            </if>
            <if test="params.paperTotalScore != null  and params.paperTotalScore != '' ">
                and assessment_paper.total_score = #{params.paperTotalScore}
            </if>
            <if test="params.paperStatus != null  and params.paperStatus != ''">
                and assessment_paper.status = #{params.paperStatus}
            </if>

            <if test="params.exampaperDeleteStart != null  and params.exampaperDeleteStart != '' ">
                <![CDATA[  and assessment_paper.is_deleted >= #{params.exampaperDeleteStart}   ]]>
            </if>
            <if test="params.exampaperDeleteEnd != null  and params.exampaperDeleteEnd != '' ">
                <![CDATA[  and assessment_paper.is_deleted <= #{params.exampaperDeleteEnd}   ]]>
            </if>
            <if test="params.paperIsDeleted != null  and params.paperIsDeleted != '' ">
                and assessment_paper.is_deleted = #{params.paperIsDeleted}
            </if>
                <!-- 判断学生的id不为空 -->
            <if test=" params.yonghuIdNotNull != '' and params.yonghuIdNotNull != null and params.yonghuIdNotNull != 'null' ">
                and a.student_id IS NOT NULL
            </if>
            <if test=" params.studentName != '' and params.studentName != null and params.studentName != 'null' ">
                and student.name like CONCAT('%',#{params.studentName},'%')
            </if>
            <if test=" params.studentPhone != '' and params.studentPhone != null and params.studentPhone != 'null' ">
                and student.phone like CONCAT('%',#{params.studentPhone},'%')
            </if>
            <if test=" params.studentIdCardNo != '' and params.studentIdCardNo != null and params.studentIdCardNo != 'null' ">
                and student.id_card_no like CONCAT('%',#{params.studentIdCardNo},'%')
            </if>
            <if test=" params.studentEmail != '' and params.studentEmail != null and params.studentEmail != 'null' ">
                and student.email like CONCAT('%',#{params.studentEmail},'%')
            </if>
        </where>

        order by a.${params.orderBy} desc 
    </select>

</mapper>